import 'package:flutter/material.dart';

class faviriteScreen extends StatefulWidget {
  faviriteScreen({Key? key}) : super(key: key);

  @override
  State<faviriteScreen> createState() => _faviriteScreenState();
}

class _faviriteScreenState extends State<faviriteScreen> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}